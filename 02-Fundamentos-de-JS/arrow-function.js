// Modo tradicional
function sumar(a, b) {
  return a + b;
}

console.log(sumar(5, 10)); //15

// Función flecha
/** 
const nombreFuncion = (param1, param2, ...) = {
    instrucciones
    return ....
}
**/

// Funcionaes flecha con una sola línea
const sumar2 = (a, b) => a + b;

console.log(sumar2(5, 10)); //15
